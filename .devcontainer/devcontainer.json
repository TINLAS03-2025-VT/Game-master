{
  "name": "Game-master ROS Humble with WireGuard",
  "dockerComposeFile": "compose.devcontainer.yaml",
  "service": "game-master-dev",
  "runServices": [
    "wireguard-client",
    "game-master-dev"
  ],
  "workspaceFolder": "/workspaces/Game-master",
  "shutdownAction": "stopCompose",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/usr/bin/python3",
        "python.analysis.extraPaths": [
          "/opt/ros/humble/local/lib/python3.10/dist-packages",
          "/opt/ros/humble/lib/python3.10/site-packages"
        ]
      }
    }
  },

  "postCreateCommand": "bash -lc 'echo \"source /opt/ros/humble/setup.bash\" >> ~/.bashrc && source /opt/ros/humble/setup.bash && python3 -c \"import rclpy; from std_msgs.msg import String, UInt64, Int32; from geometry_msgs.msg import PoseArray; print(\\\"ROS imports OK\\\")\"'"
}
